#!/bin/bash
#
# simple script to 1) determine size of new files to copy (rsync?) from location a to location b
#                  2) remove files oldest files if location b needs more space
#                  3) actually copy (rsync?) the files from location a to location b
